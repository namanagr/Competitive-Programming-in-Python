#!/usr/bin/python

def maxSumIndices(arr):
    if arr = []:
        return []
    max = float("-infinity")
    for i in range(len(arr)):
        if max <= 0:
            max = arr[i]


if __name__ == "__main__":
    arr = [-1,2,3,-5,4,7,-10]
